{
  "name": "string-state-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run",
    "compile-scripts": "node scripts/compile-scripts.mjs"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
